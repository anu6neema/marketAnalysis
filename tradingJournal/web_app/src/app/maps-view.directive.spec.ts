/* import { MapsViewDirective } from './maps-view.directive';

describe('MapsViewDirective', () => {
  it('should create an instance', () => {
    const directive = new MapsViewDirective();
    expect(directive).toBeTruthy();
  });
}); */
